version: '3.8'

services:

  front:
    build: ./
    command: npm run dev
    working_dir: /app/poster
    ports:
      - "3000:3000"
    volumes:
      - ./:/app/poster
      - /app/poster/node_modules